#!@BASH_PATH@

prefix=@prefix@
exec_prefix=@exec_prefix@
bindir=@bindir@
datadir=@datadir@
shell=@BASH_PATH@

DIALOG=`whereis dialog | awk '{ print $2 }'`

if [ -z "$DIALOG" ]; then
    @bindir@/vh_install_cli
else
    @bindir@/vh_install_gui
fi